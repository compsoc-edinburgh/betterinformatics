collections:
    sections:
        output: true

defaults:
    - scope:
        path: ""
        type: "sections"
      values:
        semester: 100 # "Other" stuff is pushed to the end
        exam_over: false
